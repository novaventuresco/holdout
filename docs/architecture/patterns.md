# Holdout — Patterns Index

See the specific files below. Each covers a distinct concern — read only what is relevant to the task at hand.

- [patterns-animation.md](patterns-animation.md) — Skia RuntimeEffect, SharedValue/useDerivedValue, time uniform, withTiming, win sequence, useAnimatedReaction, passedCount, button glow, SKSL anti-patterns. Read when: touching BattleVisual, animations, shader uniforms, or Reanimated SharedValues.

- [patterns-physics.md](patterns-physics.md) — SpatialBalance: DeviceMotion, physics intervals, AppState recentring, edgeContact lerp, wind gusts, orbOffset conversion; BattleScreen: timing pattern, stale closure/screenStateRef. Read when: touching SpatialBalance, BattleScreen physics loop, or AppState handling.

- [patterns-build.md](patterns-build.md) — Development workflow (expo-dev-client, expo start, hot-reload), build options, state management (service patterns), AsyncStorage, FlatList, useFocusEffect, crash recovery, build mistakes. Read when: build questions, starting development, service or storage work.
